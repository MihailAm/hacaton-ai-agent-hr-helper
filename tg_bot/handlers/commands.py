"""Обработчики команд бота (/start, /help)"""

from loguru import logger
from telegram import Update
from telegram.ext import ContextTypes
from telegram.constants import ParseMode


async def start_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """
    Обработчик команды /start.
    Приветствует пользователя и объясняет, как отправить резюме.
    """
    user = update.effective_user

    welcome_text = f"""
                    👋 *Привет, {user.first_name}!*
                    
                    Я — бот для приёма резюме. 
                    
                    *📝 Как отправить резюме:*
                    Просто напиши или скопируй сюда текст своего резюме.
                    
                    *📤 Что происходит после отправки:*
                    1️⃣ Твоё резюме сохраняется в нашей базе
                    2️⃣ HR специалисты знакомятся с ним
                    3️⃣ Мы свяжемся с тобой, если есть подходящие вакансии
                    
                    *🚀 Начни прямо сейчас!*
                    Отправь мне текст своего резюме в *любом формате*.
                    
                    *ℹ️ Команды:*
                    /start — это сообщение
                    /help — подробная инструкция
                    """

    await update.message.reply_text(
        welcome_text,
        parse_mode=ParseMode.MARKDOWN
    )
    logger.info(f"User {user.id} ({user.username}) started the bot")


async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """
    Обработчик команды /help.
    Подробная инструкция по использованию.
    """
    help_text = """
        *🆘 ПОМОЩЬ И ИНСТРУКЦИЯ*
        
        *📋 Как отправить резюме:*
        1. Напиши текст резюме прямо в этот чат
        2. Или скопируй из документа и вставь сюда
        
        *✅ Что должно быть в резюме:*
        • ФИО и контакты (телефон, email)
        • Желаемая должность
        • Опыт работы (если есть)
        • Образование
        • Навыки (технические и soft skills)
        
        *📎 Пример формата:*
            Иванов Иван Иванович
            Телефон: +7 999 123-45-67
            Email: example@mail.ru
        
            Желаемая должность: Python-разработчик
        
            Опыт работы:
            2022-2023 — Middle Python Developer в ООО "Рога и копыта"
            • Разработка backend на Django
            • Работа с PostgreSQL
            • Интеграция сторонних API
        
            Навыки:
            Python, Django, PostgreSQL, Git, Docker, FastAPI
        
            Образование:
            МГУ, Факультет ВМК, 2018-2022
        
            text
        
            *⚠️ Важно:*
            • Отправляй только своё резюме
            • Не присылай личные документы (паспорт и т.д.)
            • Минимальная длина — 50 символов
        
            *🛠️ Команды:*
            /start — начать работу
            /help — эта инструкция
            """

    await update.message.reply_text(
        help_text,
        parse_mode=ParseMode.MARKDOWN
    )
